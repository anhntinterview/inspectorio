import * as React from 'react';

const NotFound: React.FunctionComponent = () => {
    return (
        <div>
            <h1>Sorry, we can not found your request page!</h1>
        </div>
    );
}

export default NotFound
